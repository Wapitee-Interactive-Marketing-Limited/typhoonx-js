---
'@wapitee/typhoonx-hydrogen': minor
'@wapitee/typhoonx-react': minor
---

Send GA4 `_ga` as `ga_client_id`, `_ga_<ID>` as `ga_session_id` when `measurementId` is set, and Google Ads `gclid` (from `_gcl_aw` or the page URL), `gbraid` and `wbraid` on every event.
